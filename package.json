{
  "name": "pomodoro",
  "version": "1.0.0", 
  "description": "Pomodoro Timer with Blessed UI",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm start",
    "package": "npm run build && pkg dist/index.js --targets node18-linux-x64 --output pomodoro-linux --config package.json",
    "package-all": "npm run build && pkg dist/index.js --targets node18-linux-x64,node18-win-x64,node18-macos-x64 --output pomodoro --config package.json"
  },
  "pkg": {
    "assets": [
      "node_modules/blessed/**/*"
    ],
    "scripts": [
      "dist/**/*.js"
    ],
    "targets": [
      "node18-linux-x64"
    ]
  },
  "dependencies": {
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25", 
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
